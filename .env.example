# API-Football (API-SPORTS) key — powers live tournament stats.
# Get a free key at https://www.api-football.com/  (or via RapidAPI).
# On Vercel: Project → Settings → Environment Variables → add APISPORTS_KEY.
# For local dev with `vercel dev`, copy this file to `.env` and fill it in.
APISPORTS_KEY=
